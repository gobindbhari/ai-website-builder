import { reactBasePrompt } from "@/constants/react";
import { parseXmlFiles } from "@/utils/step";
import { NextResponse } from "next/server";

export const tempResponse = "We’ve built a production‑ready Todo app using the existing React + TypeScript + Tailwind stack, lucide‑react icons, and an Unsplash hero image. All new code is split into small, reusable components and typed with TypeScript.\n\n<boltArtifact id=\"todo-app\" title=\"Full Todo App Implementation\">\n  <boltAction type=\"file\" filePath=\"src/types.ts\">\n    export type Filter = 'All' | 'Active' | 'Completed';\n\n    export interface Todo {\n      id: string;\n      text: string;\n      completed: boolean;\n    }\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/components/TodoItem.tsx\">\n    import React from 'react';\n    import { CheckSquare, Square, Trash2 } from 'lucide-react';\n    import { Todo } from '../types';\n\n    type Props = {\n      todo: Todo;\n      toggle: (id: string) => void;\n      deleteTodo: (id: string) => void;\n    };\n\n    export default function TodoItem({ todo, toggle, deleteTodo }: Props) {\n      return (\n        <li className=\"flex items-center justify-between p-2 border-b last:border-b-0\">\n          <div\n            className=\"flex items-center space-x-2 cursor-pointer\"\n            onClick={() => toggle(todo.id)}\n          >\n            {todo.completed ? (\n              <CheckSquare className=\"text-green-500\" size={18} />\n            ) : (\n              <Square className=\"text-gray-400\" size={18} />\n            )}\n            <span\n              className={\n                todo.completed ? 'line-through text-gray-500' : ''\n              }\n            >\n              {todo.text}\n            </span>\n          </div>\n          <button\n            onClick={() => deleteTodo(todo.id)}\n            className=\"text-red-500 hover:text-red-700\"\n            aria-label=\"Delete todo\"\n          >\n            <Trash2 size={18} />\n          </button>\n        </li>\n      );\n    }\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/components/AddTodo.tsx\">\n    import React, { useState } from 'react';\n    import { Plus } from 'lucide-react';\n\n    type Props = {\n      addTodo: (text: string) => void;\n    };\n\n    export default function AddTodo({ addTodo }: Props) {\n      const [text, setText] = useState('');\n\n      const handleSubmit = (e: React.FormEvent) => {\n        e.preventDefault();\n        const trimmed = text.trim();\n        if (!trimmed) return;\n        addTodo(trimmed);\n        setText('');\n      };\n\n      return (\n        <form onSubmit={handleSubmit} className=\"flex mb-4\">\n          <input\n            className=\"flex-1 border rounded-l px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500\"\n            placeholder=\"What needs to be done?\"\n            value={text}\n            onChange={e => setText(e.target.value)}\n          />\n          <button\n            type=\"submit\"\n            className=\"bg-blue-500 hover:bg-blue-600 text-white px-4 rounded-r flex items-center\"\n            aria-label=\"Add todo\"\n          >\n            <Plus size={20} />\n          </button>\n        </form>\n      );\n    }\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/components/FilterTabs.tsx\">\n    import React from 'react';\n    import { Filter } from '../types';\n\n    type Props = {\n      current: Filter;\n      setFilter: (f: Filter) => void;\n    };\n\n    export default function FilterTabs({ current, setFilter }: Props) {\n      const filters: Filter[] = ['All', 'Active', 'Completed'];\n\n      return (\n        <div className=\"flex space-x-4 mb-4\">\n          {filters.map(f => (\n            <button\n              key={f}\n              onClick={() => setFilter(f)}\n              className={`px-3 py-1 rounded ${\n                current === f\n                  ? 'bg-blue-500 text-white'\n                  : 'bg-gray-200 hover:bg-gray-300'\n              }`}\n            >\n              {f}\n            </button>\n          ))}\n        </div>\n      );\n    }\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/App.tsx\">\n    import React, { useEffect, useState } from 'react';\n    import { Todo, Filter } from './types';\n    import AddTodo from './components/AddTodo';\n    import TodoItem from './components/TodoItem';\n    import FilterTabs from './components/FilterTabs';\n    import { v4 as uuidv4 } from 'uuid';\n\n    function App() {\n      const [todos, setTodos] = useState<Todo[]>([]);\n      const [filter, setFilter] = useState<Filter>('All');\n\n      // Load from localStorage once\n      useEffect(() => {\n        const stored = localStorage.getItem('todos');\n        if (stored) {\n          setTodos(JSON.parse(stored));\n        }\n      }, []);\n\n      // Persist whenever todos change\n      useEffect(() => {\n        localStorage.setItem('todos', JSON.stringify(todos));\n      }, [todos]);\n\n      const addTodo = (text: string) => {\n        const newTodo: Todo = {\n          id: uuidv4(),\n          text,\n          completed: false,\n        };\n        setTodos(prev => [newTodo, ...prev]);\n      };\n\n      const toggleTodo = (id: string) => {\n        setTodos(prev =>\n          prev.map(t =>\n            t.id === id ? { ...t, completed: !t.completed } : t\n          )\n        );\n      };\n\n      const deleteTodo = (id: string) => {\n        setTodos(prev => prev.filter(t => t.id !== id));\n      };\n\n      const filteredTodos = todos.filter(t => {\n        if (filter === 'All') return true;\n        if (filter === 'Active') return !t.completed;\n        return t.completed;\n      });\n\n      const remaining = todos.filter(t => !t.completed).length;\n\n      return (\n        <div className=\"min-h-screen bg-gray-100 flex flex-col items-center py-8\">\n          {/* Hero image */}\n          <img\n            src=\"https://images.unsplash.com/photo-1521737604893-d14cc237f11d?auto=format&fit=crop&w=1200&q=80\"\n            alt=\"Workspace\"\n            className=\"w-full max-w-4xl h-64 object-cover rounded-lg mb-8 shadow-lg\"\n          />\n\n          <div className=\"w-full max-w-2xl bg-white rounded-lg shadow-md p-6\">\n            <h1 className=\"text-3xl font-bold text-center mb-6\">\n              My Todo List\n            </h1>\n\n            <AddTodo addTodo={addTodo} />\n\n            <FilterTabs current={filter} setFilter={setFilter} />\n\n            {filteredTodos.length === 0 ? (\n              <p className=\"text-center text-gray-500\">\n                No tasks {filter === 'All' ? '' : `for \"${filter}\"`} yet.\n              </p>\n            ) : (\n              <ul className=\"divide-y divide-gray-200\">\n                {filteredTodos.map(todo => (\n                  <TodoItem\n                    key={todo.id}\n                    todo={todo}\n                    toggle={toggleTodo}\n                    deleteTodo={deleteTodo}\n                  />\n                ))}\n              </ul>\n            )}\n\n            <p className=\"mt-4 text-sm text-gray-600 text-center\">\n              {remaining} item{remaining !== 1 ? 's' : ''} left\n            </p>\n          </div>\n\n          <footer className=\"mt-8 text-center text-gray-500\">\n            Built with React, TypeScript, Tailwind CSS &amp; Lucide icons\n          </footer>\n        </div>\n      );\n    }\n\n    export default App;\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/main.tsx\">\n    import { StrictMode } from 'react';\n    import { createRoot } from 'react-dom/client';\n    import App from './App';\n    import './index.css';\n\n    createRoot(document.getElementById('root')!).render(\n      <StrictMode>\n        <App />\n      </StrictMode>\n    );\n  </boltAction>\n\n  <boltAction type=\"file\" filePath=\"src/index.css\">\n    @tailwind base;\n    @tailwind components;\n    @tailwind utilities;\n\n    /* Custom utilities (optional) */\n    .line-through {\n      @apply line-through;\n    }\n  </boltAction>\n\n  <boltAction type=\"shell\">\n    npm install uuid\n  </boltAction>\n</boltArtifact>"

export async function GET() {
    const res = parseXmlFiles(reactBasePrompt)
    const restemp = parseXmlFiles(tempResponse)
    const message = tempResponse.split("<boltArtifact")[0].replaceAll('\n\n', "")
    const titleMatch = tempResponse.match(/title="([^"]*)"/)?.[1];

    const projectFiles = [
        ...res.map((f) => {
            const updated = restemp.find((t) => t.title === f.title);
            return updated ?? f;
        }),
        ...restemp.filter(
            (t) => !res.some((f) => f.title === t.title)
        ),
    ];

    const newP = projectFiles.map(p => {
        return {
            ...p,
            code: p.code || "",
            path: p.path || "",
            type: "File",
        }
    })


    return NextResponse.json({ message, newP, titleMatch, projectFiles, res, restemp })
}